import links from '@/lib/resources/agama-links.json'
const escape = (value: string) => value.replace(/[&<>"']/g, c => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c]!))
export async function GET() {
 const rows = links.map(l => `<tr><td>${escape(l.product || l.category)}</td><td>${l.english ? 'EN' : 'ES'}</td><td><a href="${escape(l.url)}">${escape(l.label)}</a></td></tr>`).join('')
 const us = links.filter(l => l.usa).map(l => `<li><a href="${escape(l.url)}">${escape(l.label)}</a></li>`).join('')
 const html = `<!doctype html><html lang="es"><meta charset="utf-8"><meta name="viewport" content="width=device-width"><title>AGAMA — Informe de enlaces</title><style>body{font:16px/1.6 system-ui;max-width:1050px;margin:40px auto;padding:0 24px;color:#182b40}table{border-collapse:collapse;width:100%;font-size:14px}td,th{padding:12px;border-bottom:1px solid #ddd;text-align:left}a{color:#165990}h1,h2{line-height:1.2}@media print{tr{break-inside:avoid}}</style><body><p>WF-STUDIO · AGAMA</p><h1>Informe de enlaces y contenido para EEUU</h1><p>Entrega: 23 de septiembre de 2026. Última comprobación documentada: 18 de septiembre de 2026.</p><p>83 enlaces: 72 recursos de producto ES/EN, seis artículos educativos en inglés, dos páginas de servicio y tres páginas de categoría. Los 47 enlaces en inglés están incluidos en las 83 URLs.</p><p>El 18 de septiembre las 83 URLs respondieron HTTP 200, con canonical y sin meta noindex. No es una nueva auditoría ni acredita posicionamiento o tráfico desde EEUU. Son páginas de AGAMA, no backlinks externos.</p><h2>Contenido específico para EEUU</h2><ul>${us}</ul><h2>Inventario completo</h2><table><thead><tr><th>Contenido</th><th>Idioma</th><th>Enlace</th></tr></thead><tbody>${rows}</tbody></table></body></html>`
 return new Response(html, { headers: { 'Content-Type': 'text/html; charset=utf-8', 'Content-Disposition': 'attachment; filename="AGAMA-informe-enlaces-2026-09.html"', 'X-Content-Type-Options': 'nosniff' } })
}
